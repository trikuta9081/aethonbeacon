FROM node:22-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV HOST=0.0.0.0

# Serve pre-built dist committed to repo (no build step — instant deploys)
COPY dist ./dist
COPY scripts/static-server.mjs ./scripts/static-server.mjs

EXPOSE 3000

CMD ["node", "scripts/static-server.mjs"]
